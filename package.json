{
  "name": "soundcn",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean",
    "registry:build": "turbo run registry:build",
    "registry:enrich": "turbo run registry:enrich"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.9.2"
  },
  "packageManager": "bun@1.3.2"
}
